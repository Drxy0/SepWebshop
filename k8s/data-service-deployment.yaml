apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-service
  template:
    metadata:
      labels:
        app: data-service
    spec:
      containers:
        - name: data-service
          image: ${ACR_NAME}.azurecr.io/data-service:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /d/test/ping
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /d/test/ping
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: data-service-clusterip
spec:
  type: ClusterIP
  selector:
    app: data-service
  ports:
    - name: data-service
      port: 80
      targetPort: 8080
