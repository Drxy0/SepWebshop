apiVersion: apps/v1
kind: Deployment
metadata:
  name: card-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: card-service
  template:
    metadata:
      labels:
        app: card-service
    spec:
      containers:
        - name: card-service
          image: ${ACR_NAME}.azurecr.io/card-service:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /ca/test/ping
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ca/test/ping
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: card-service-clusterip
spec:
  type: ClusterIP
  selector:
    app: card-service
  ports:
    - name: card-service
      port: 80
      targetPort: 8080
